message: Greetings from the COSTBO-ACCOUNT-QUERY-SIDE microservice [using the PRODUCTION config].

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
  instance:
    preferIpAddress: true
    
spring:
    application:
        exchange: accounts.events.fanout.exchange
        queue: event.stream
        terminal: accounts.axon.terminal
    rabbitmq:
        hostname: rabbitmq
        username: test
        password: password
    data:
        mongodb:
            uri: mongodb://mongodb:27017/accounts_qry

